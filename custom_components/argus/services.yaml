save_ui:
  name: Save Argus UI
  description: Persists zones and Argus panel configuration created from the UI.

panic:
  name: Panic alarm
  description: Immediately triggers the alarm in triggered mode.
  fields:
    entity_id:
      required: true
      example: alarm_control_panel.argus
      selector:
        entity:
          domain: alarm_control_panel
