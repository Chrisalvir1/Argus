/** Argus v2.0.50 — single alarm visual layer for entries (replaces competing v2042-46 paints). */
const STYLE_ID='argus-v2050-alarm-visuals';
const ACTIVE={armed_home:/EN CASA|HOME/i,armed_away:/AUSENTE|AWAY/i,armed_night:/NOCHE|NIGHT/i,armed_vacation:/VACACIONES|VACATION/i};
function stateOf(panel,item){const id=item?.entity_id||item?.alarm_entity_id;return String(item?.state||item?.alarm_state||item?.attributes?.state||panel._hass?.states?.[id]?.state||'').toLowerCase()}
function paint(button,kind,active){button.classList.toggle('argus-action-active',active);button.dataset.argusAction=kind;const color=kind==='sos'?'linear-gradient(110deg,#ff453a,#ff2d55)':kind==='disarm'?'linear-gradient(110deg,#34c759,#16863a)':'linear-gradient(110deg,rgba(67,180,255,.72),rgba(52,120,212,.68))';button.style.setProperty('background',active?color:'rgba(255,255,255,.105)','important');button.style.setProperty('background-image',active?color:'linear-gradient(135deg,rgba(255,255,255,.12),rgba(255,255,255,.055))','important');button.style.setProperty('border',active?'1px solid rgba(255,255,255,.38)':'1px solid rgba(255,255,255,.14)','important');button.style.setProperty('box-shadow',active?'0 8px 24px rgba(0,0,0,.24)':'inset 0 1px 0 rgba(255,255,255,.08)','important')}
function ensureStyles(panel){const root=panel.shadowRoot;if(!root)return;let style=root.getElementById(STYLE_ID);if(!style){style=document.createElement('style');style.id=STYLE_ID;root.appendChild(style)}style.textContent=`\n.entry .liquid-stack .liquid-btn{border-radius:13px!important;color:rgba(255,255,255,.92)!important}\n.argus-disarm-btn{grid-column:1/-1!important;width:100%!important;min-height:54px!important;background:linear-gradient(110deg,#34c759,#16863a)!important;border:1px solid rgba(134,239,172,.78)!important;color:#fff!important}\n.entry.argus-arming .entry-icon>svg,.entry.argus-waiting .entry-icon>svg{transform-origin:center!important;animation:argusArmingShield 1.05s ease-in-out infinite!important;filter:drop-shadow(0 0 26px rgba(255,184,57,.95)) saturate(1.35)!important}\n.argus-shield-status{display:block;margin:7px auto 0;padding:5px 10px;width:max-content;max-width:180px;border:1px solid rgba(255,184,57,.45);border-radius:999px;background:rgba(255,149,0,.13);color:#ffd27a;font-size:9px;font-weight:800;letter-spacing:.12em;text-align:center;animation:argusArmingLabel 1.05s ease-in-out infinite}\n.entry .console-sensor.argus-blocking,.entry .console-sensor.argus-triggered-sensor{border-color:#ff8a1f!important;background:linear-gradient(135deg,rgba(249,115,22,.32),rgba(194,65,12,.18))!important;animation:argusTriggeredSensor .72s ease-in-out infinite!important}\n@keyframes argusArmingShield{0%,100%{opacity:.55;transform:scale(.94)}50%{opacity:1;transform:scale(1.07)}}\n@keyframes argusArmingLabel{0%,100%{opacity:.62}50%{opacity:1}}\n@keyframes argusTriggeredSensor{0%,100%{opacity:.62}50%{opacity:1}}\n`;}
function apply(panel){if(!panel.shadowRoot)return;ensureStyles(panel);[...(panel.shadowRoot.querySelectorAll('.entry')||[])].forEach((entry,index)=>{const item=panel._dashboard?.entries?.[index]||{};const attrs=item.attributes||panel._hass?.states?.[item.entity_id]?.attributes||{};const state=stateOf(panel,item);const blocking=attrs.arming_blocking_sensors||[];const waiting=state==='arming'||Boolean(attrs.arming_waiting_for_sensors)||Boolean(blocking.length);entry.classList.toggle('argus-arming',waiting);entry.classList.toggle('argus-waiting',waiting);const icon=entry.querySelector('.entry-icon');let label=entry.querySelector('.argus-shield-status');if(waiting&&icon){if(!label){label=document.createElement('span');label.className='argus-shield-status';icon.insertAdjacentElement('afterend',label)}label.textContent=blocking.length?`ESPERANDO ${blocking.length} SENSOR(ES)`:(state==='arming'?'ARMANDO…':'ESPERANDO SENSORES')}else label?.remove();const stack=entry.querySelector('.liquid-stack');if(stack){let btn=stack.querySelector('.argus-disarm-btn');if(!btn){btn=document.createElement('button');btn.type='button';btn.className='liquid-btn argus-disarm-btn';btn.innerHTML='<span>⏻</span><b>DESARMAR / OFF</b>';btn.onclick=async()=>{if(!item)return;btn.disabled=true;try{await panel._send('argus/perform_alarm_action',{entry_id:item.entry_id,action:'disarm'});await panel._load?.()}finally{btn.disabled=false}};const sos=[...stack.children].find(x=>/SOS|PÁNICO|PANIC/i.test(x.textContent||''));if(sos)stack.insertBefore(btn,sos);else stack.appendChild(btn)}[...stack.querySelectorAll('.liquid-btn,button')].forEach(b=>{const text=String(b.textContent||'');if(/SOS|PÁNICO|PANIC/i.test(text))paint(b,'sos',Boolean(attrs.argus_panic_active));else if(/DESARMAR|DISARM|OFF/i.test(text))paint(b,'disarm',state==='disarmed');else{const match=Object.entries(ACTIVE).find(([,rx])=>rx.test(text));if(match)paint(b,match[0],state===match[0])}})}const triggered=new Set((attrs.triggered_sensors||[]).map(String));const names=new Set(blocking.map(id=>String(panel._dashboard?.available_entities?.find?.(x=>x.entity_id===id)?.name||id).toLocaleLowerCase()));entry.querySelectorAll('.console-sensor').forEach(row=>{const text=String(row.querySelector('.console-sensor-name')?.textContent||row.textContent||'').toLocaleLowerCase();const isBlock=[...names].some(n=>text.includes(n)||n.includes(text));const isTrig=[...triggered].some(id=>text.includes(String(id).toLocaleLowerCase()));row.classList.toggle('argus-blocking',isBlock);row.classList.toggle('argus-triggered-sensor',isTrig)})})}
export function applyV2050AlarmVisuals(C){if(!C||C.__v2050AlarmVisuals)return;C.__v2050AlarmVisuals=true;const p=C.prototype,connected=p.connectedCallback,load=p._load,render=p._renderEntries;p.connectedCallback=function(){const r=connected?.call(this);apply(this);return r};p._load=async function(...a){const r=await load?.apply(this,a);apply(this);return r};p._renderEntries=function(...a){const r=render?.apply(this,a);apply(this);return r}}
