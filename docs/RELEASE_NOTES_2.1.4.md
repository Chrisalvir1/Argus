# Argus v2.1.4 — Real-time History Translations and Custom Pulse Colors
- Added real-time translation mapping in Javascript for audit logs (handling Roles, Modes, Settings dynamically when changing languages).
- Fixed siren/SOS light brightness pulse color distortion by mathematically scaling RGB values instead of forcing brightness percentages.
- Fixed startup layout flicker by hiding the dashboard container until session validation is complete.
