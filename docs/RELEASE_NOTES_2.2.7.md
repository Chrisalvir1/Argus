# Release Notes - v2.2.7

- Fix slide-to-action backend integration payload (use real entry_id).
- Fix action parameter for panic stop in backend websocket API.
- Fix UI height stretching in compact and dashboard views.
- Ensure sliders reliably mount without reverting to old buttons in React dashboard.
